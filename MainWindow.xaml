<Window x:Class="TaskManagerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManagerApp"
        mc:Ignorable="d"
        Title="Task Manager" Height="350" Width="400">

    <StackPanel Margin="20">
        <TextBox Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}"  Margin="0 0 0 10"/>
        <Button x:Name="AddButton" Content="追加" Command="{Binding AddTaskCommand}" Margin="0 0 0 10" />
        <Button x:Name="DeleteButton" IsEnabled="{Binding CanDelete}" Content="削除" Command="{Binding DeleteTaskCommand}" Margin="0 0 0 10" />
        <ListBox ItemsSource="{Binding Tasks}"
                 SelectedItem="{Binding SelectedTask, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
               
        </ListBox>

    </StackPanel>
</Window>
